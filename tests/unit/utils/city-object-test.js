import cityObject from '../../../utils/city-object';
import { module, test } from 'qunit';

module('Unit | Utility | city object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cityObject();
  assert.ok(result);
});
