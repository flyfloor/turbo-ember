import provinceObject from '../../../utils/province-object';
import { module, test } from 'qunit';

module('Unit | Utility | province object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = provinceObject();
  assert.ok(result);
});
