import areaObject from '../../../utils/area-object';
import { module, test } from 'qunit';

module('Unit | Utility | area object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = areaObject();
  assert.ok(result);
});
