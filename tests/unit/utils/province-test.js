import province from '../../../utils/province';
import { module, test } from 'qunit';

module('Unit | Utility | province');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = province();
  assert.ok(result);
});
