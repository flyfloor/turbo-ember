import { cityName } from '../../../helpers/city-name';
import { module, test } from 'qunit';

module('Unit | Helper | city name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cityName(42);
  assert.ok(result);
});
