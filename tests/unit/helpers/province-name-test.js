import { provinceName } from '../../../helpers/province-name';
import { module, test } from 'qunit';

module('Unit | Helper | province name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = provinceName(42);
  assert.ok(result);
});
