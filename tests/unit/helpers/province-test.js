import { province } from '../../../helpers/province';
import { module, test } from 'qunit';

module('Unit | Helper | province');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = province(42);
  assert.ok(result);
});
