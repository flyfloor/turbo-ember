import { areaName } from '../../../helpers/area-name';
import { module, test } from 'qunit';

module('Unit | Helper | area name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = areaName(42);
  assert.ok(result);
});
