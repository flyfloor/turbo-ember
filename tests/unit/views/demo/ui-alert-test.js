import { moduleFor, test } from 'ember-qunit';

moduleFor('view:demo/ui-alert', 'Unit | View | demo/ui alert');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
